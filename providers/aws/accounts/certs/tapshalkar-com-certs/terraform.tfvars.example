account_email = "root+certs@example.com"
region        = "us-east-1"

# Optional overrides (defaults shown)
# budget_amount      = 10
# budget_thresholds  = [0.5, 0.9, 1.0]
# notification_email = "override@example.com"  # inherits from management account if unset
